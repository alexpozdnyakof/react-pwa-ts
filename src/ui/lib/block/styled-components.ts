import styled from 'styled-components'

/**
 * Зачем нужен блок?
 * Чтобы пробросить токены: цвета, шрифт
 */

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const BaseBlock = styled.div(({ theme }) => ({}))
